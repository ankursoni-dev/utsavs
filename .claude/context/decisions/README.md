# Architectural Decision Records (ADRs) live here.
# Format: `NNNN-slug.md` where NNNN is a zero-padded sequence number.
# The CONTEXT-CURATOR subagent creates ADRs after cross-cutting decisions.
# See `.claude/agents/CONTEXT-CURATOR.md` for the ADR format.
